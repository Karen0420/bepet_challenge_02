import string
import random

def generator_password():
    caracteres = string.ascii_letters + string.digits + string.punctuation
    return ''.join(random.choice(caracteres) for _ in range(0,16))

print(generator_password())
